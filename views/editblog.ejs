<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Blog</title>
  <%- include('./partials/styles.ejs') %>
</head>
<body>
  <%- include('./partials/topbar.ejs') %>

  <form id="editBlogForm" class="blog-form" method="POST" action="/updateblog?blogId=<%= blogData._id %>">
    <h2>Edit Blog</h2>

    <input id="blogTitle" type="text" name="title" value="<%= blogData.title %>" placeholder="Enter Blog Title" required />
    <textarea id="blogBody" name="body" placeholder="Enter Blog Body" rows="8" required><%= blogData.body %></textarea>
    <input id="editBlogBtn" type="submit" value="Save Changes" />

    <% if (message) { %>
      <p class="message"><%= message %></p>
    <% } %>
  </form>
</body>
</html>
